" Editor config lives in ~/.config/nvim/init.lua — use nvim.
" Plain Vim fallback (indent + line numbers only):
set expandtab shiftwidth=4 tabstop=4 softtabstop=4 autoindent smartindent number relativenumber
